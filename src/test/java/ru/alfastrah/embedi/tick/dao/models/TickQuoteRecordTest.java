package ru.alfastrah.embedi.tick.dao.models;

public class TickQuoteRecordTest {

    public static TickQuoteRecord makeFake(Integer seed) {
        TickQuoteRecord tRecord = new TickQuoteRecord();
        tRecord.setAddress(address);
    }

}
